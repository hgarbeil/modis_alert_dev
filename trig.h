#ifndef TRIG_H
#define TRIG_H
#include <cmath>

const double DTOR=(M_PI / 180.);
#define RADOF(deg)  static_cast<double>(DTOR * (deg))
#define RADOF(deg)  static_cast<double>(DTOR * (deg))

#endif // TRIG_H
